name: semantic-id-generation
channels:
  - conda-forge  # Fallback for other libraries (Polars, Transformers, etc.)
  - defaults
dependencies:
  # Core Python
  - python=3.14
  - pip
  
  # PyTorch
  - pytorch=2.12 # supports fused adagrad on CUDA and fixes gumbel_softmax on MPS
  - torchvision
  - pytorch_geometric
  
# Data Science & Processing
  - numpy
  - pandas
  - polars
  - scikit-learn
  - scipy
  - pyarrow
  
  # NLP & Transformers
  - sentence-transformers
  - transformers
  - tokenizers
  
  # ML Utilities
  - einops
  - hyperopt
  - tqdm
  - python-dotenv
  
  # Config & Logging
  - hydra-core
  - omegaconf
  - pyyaml
  - wandb
  - torchmetrics
  
  # Testing
  - pytest
  
  # Notebooks & Analysis
  - jupyter
  - ipykernel
  - matplotlib
  - seaborn
  - marimo
  - plotly